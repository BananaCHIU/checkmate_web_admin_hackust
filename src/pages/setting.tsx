import Layout from 'Layouts';
import MessageText from '@paljs/ui/Chat/MessageText';

const Setting = () => {
  return (
    <Layout title="Setting">
      <MessageText message="Setting Page" />
    </Layout>
  );
};

export default Setting;
